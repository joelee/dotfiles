{{ $username := promptString "User name for .gitconfig" -}}
{{ $email := promptString "Your Email for .gitconfig" -}}

[secret]
command = "get-secret.sh"

[template]
options = ["missingkey=zero"]

[data]
username = {{ $username | quote }}
email = {{ $email | quote }}
